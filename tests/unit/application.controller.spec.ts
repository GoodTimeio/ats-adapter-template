describe('ApplicationController', () => {
    // Check out the framework docs for testing info: https://docs.nestjs.com/fundamentals/testing.
    // We usually mock network requests via [nock](https://www.npmjs.com/package/nock).
    it.todo('test well :)');
});
